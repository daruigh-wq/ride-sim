# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Virtual environments
venv/
.venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# PyInstaller — ignore auto-generated specs but keep our hand-crafted one
*.spec
!ride_sim.spec

# Qt
*.qmlc
*.jsc

# App settings (user-specific)
.ride_sim_settings.json

# Recorded activities (user data)
ride_*.tcx

# Media binaries — videos and GoPro raw should never live in the source repo
*.mp4
*.mov
*.avi
*.mkv
*.360

# GoPro-named test footage and yaw-analysis CSVs
GS*.tcx
GS*.csv

# Local test clips (mp4 already covered above; this catches the .tcx + .csv)
test_clip*

# Yaw / FOV calibration experiments — kept locally, not part of the app
scripts/out/
scripts/yaw_tune.py
scripts/curve_smoke.py
scripts/reframe_360.sh
